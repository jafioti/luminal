[package]
name = "luminal_macro"
version = "0.2.0"
edition = "2021"
description = "Procedural macros for luminal"
license = "MIT OR Apache-2.0"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["derive", "full"] }
quote = "1.0"
